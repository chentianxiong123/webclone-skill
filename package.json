{
  "name": "web-clone-monorepo",
  "version": "1.0.4",
  "private": true,
  "packageManager": "pnpm@10.8.0",
  "description": "Monorepo for web-clone snapshot tool",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:cli": "pnpm --filter web-clone-cli dev",
    "test": "turbo run test",
    "test:run": "turbo run test",
    "test:unit": "pnpm --filter @web-clone/core test:unit",
    "test:integration": "pnpm --filter web-clone-cli test:integration",
    "test:all": "pnpm test:unit && pnpm test:integration",
    "test:coverage": "turbo run test:coverage",
    "test:clean": "pnpm --filter web-clone-cli test:clean",
    "lint": "turbo run lint",
    "lint:fix": "pnpm --recursive lint:fix",
    "clean": "turbo run clean",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "snapshot": "pnpm dev:cli --",
    "browsers:check": "node scripts/check-browsers.mjs",
    "browsers:check:playwright": "node scripts/check-playwright.mjs",
    "browsers:check:puppeteer": "node scripts/check-puppeteer.mjs",
    "browsers:install": "npx playwright install chromium",
    "browsers:install:all": "npx playwright install",
    "test:e2e": "vitest run --config tests/e2e/vitest.config.ts tests/e2e/spa-crawl-e2e.test.ts",
    "test:e2e:all": "pnpm build && pnpm test:e2e",
    "test:e2e:build": "npx tsx tests/e2e/build-fixtures.ts",
    "test:e2e:regression": "bash scripts/e2e-regression-check.sh"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "@web-clone/adapter-playwright": "workspace:*",
    "@web-clone/core": "workspace:*",
    "eslint": "^10.7.0",
    "jsdom": "^29.1.1",
    "playwright": "1.58.2",
    "prettier": "^3.6.0",
    "tsx": "^4.23.0",
    "turbo": "^2.5.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.63.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
